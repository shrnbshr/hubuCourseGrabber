# 湖北大学强智教务自动抢课 —— 保姆级操作指南

本指南专为零基础同学编写，按照步骤操作，两分钟内即可完成配置并开始自动抢课。

---

## 路线一：终极省心法（油猴插件自动运行，强烈推荐 ⭐️⭐️⭐️⭐️⭐️）

> **优势**：配置一次即可长期生效。进入选课页面后，控制面板会自动弹出，在表格里直接点击想抢课程后面的 **【⚡ 抢这门】** 即可开抢。**不需要每次按 F12，不需要复制代码，不需要手动敲课程代码**。

### 第一步：开启浏览器的"开发者模式"（极易忽略的必须步骤）

由于新版 Chrome 和 Edge 浏览器的安全策略限制，若不开启此开关，油猴将无法在页面中运行脚本：

1. 打开 Chrome 浏览器，在地址栏输入：`chrome://extensions/`（如果是 Edge 浏览器请输入 `edge://extensions/`）并回车。
2. 查看页面右上角，找到 **"开发者模式"（Developer Mode）** 开关。
3. **将开关开启**。

### 第二步：安装油猴插件

1. 如果电脑尚未安装油猴扩展：
   - **Edge 浏览器**：直接在 Edge 扩展商店搜索 `Tampermonkey` 点击安装。
   - **Chrome 浏览器**：在 Chrome 网上应用店搜索 `Tampermonkey` 安装（若无法访问，可在国内扩展镜像站下载后拖入扩展页面安装）。

### 第三步：添加抢课脚本

1. 点击浏览器右上角拼图图标旁边出现的 **黑色油猴图标**。
2. 在下拉菜单中点击 **"添加新脚本"**。
3. 进入代码编辑器后，按键盘 **`Ctrl + A`**（全选里面的默认代码），按 **`Backspace / Delete`** 全部清空。
4. 打开本地文件 [`hubuCourseGrabber.user.js`](hubuCourseGrabber.user.js)，按 **`Ctrl + A`** 全选复制里面的全部内容。
5. 粘贴到油猴编辑器中，按键盘 **`Ctrl + S`** 保存（或点击编辑器左上角"文件" -> "保存"）。
6. 当脚本列表里出现"湖北大学强智教务自动抢课助手"且开关为开启状态时，说明配置完成。

### 第四步：进入选课页面一键开抢

1. 正常登录教务系统后，直接进入选课入口页面：
   - 选课直达链接(示例)：`https://jwxt.hubu.edu.cn/jsxsd/xsxk/xsxk_index?xxxxxxxxxx=xxxxxxxxxxxxxxxxxxxxxxxxx`
2. **页面右上角会自动弹出【🎓 HUBU 抢课助手】控制面板**。
3. 在筛选条件中：
   - **取消勾选"过滤已满课程"**（确保即使名额为 0 的课也会在列表中展示）。
   - 点击 **"查询"** 按钮。
4. 在下方的数据表格中找到目标课程，查看该行最右侧的操作列：
   - 会看到一个蓝色的 **【⚡ 抢这门】** 按钮。
   - **直接鼠标点击【⚡ 抢这门】**。
5. 点击后，面板会自动加入该课程并启动自动监控（状态显示为"正在自动抢课中..."）。脚本会在后台每隔 1.2 秒自动轮询检测名额；一旦有人退课，毫秒级自动提交并跳过所有确认弹窗。

---

## 路线二：无需安装插件法（控制台临时粘贴，适合机房电脑 ⭐️⭐️⭐️）

> **适用场景**：在公用机房或无法安装扩展的临时电脑上使用。

### 第一步：进入选课列表

1. 打开浏览器登录教务系统进入选课页，取消勾选"过滤已满课程"，点击"查询"调出表格。

### 第二步：打开 Console 控制台

1. 在网页上按键盘 **`F12`**（若无效按 `Fn + F12`，或鼠标右键页面空白处 -> 检查）。
2. 在弹出的调试窗口上方，点击切换到 **Console**（控制台）标签页。

### 第三步：复制代码运行

1. 打开本地文件 [`hubuCourseGrabber.js`](hubuCourseGrabber.js)，按 `Ctrl + A` 全选复制。
2. 回到控制台最下方光标处，按 `Ctrl + V` 粘贴并回车。
3. 页面上会弹出悬浮控制面板，表格每一行右侧也会自动注入【⚡ 抢这门】按钮。

### 第四步：启动抢课

- 在表格中找到目标课程，直接点击该行右侧的 **【⚡ 抢这门】** 即可开抢。

> **提示**：控制台版本的面板功能更丰富，支持手动输入课程代码、优先级、时间/教师过滤和定时开抢。

---

## 高级功能：定时开抢（仅控制台版）

> ⚠️ 此功能仅在**路线二（控制台版 `hubuCourseGrabber.js`）**中可用，油猴版不包含此功能。

若选课系统尚未开放（例如明天 14:00 才开抢），可以提前挂机：

1. 提前登录教务系统并进入选课页，按路线二的步骤粘贴运行控制台版脚本，确保悬浮窗正常显示。
2. 点击悬浮窗中的 **"⏰ 定时开抢"** 时间选择框：
   - 选取开抢的日期与具体时间（如：`2026-09-05 14:00`）。
   - 点击 **【设定】** 按钮。
3. 界面会出现毫秒级紫色倒计时。
4. 到达设定时间的一瞬间，脚本会自动唤醒并向服务器发起高频选课冲刺。

---

## 📋 两版功能对比

| 功能                     | 油猴版 (路线一) | 控制台版 (路线二) |
| ------------------------ | :-------------: | :---------------: |
| ⚡ 表格行内一键抢课      |       ✅        |        ✅         |
| 自动轮询 + 静默刷新      |       ✅        |        ✅         |
| 弹窗自动放行             |       ✅        |        ✅         |
| 手动输入课程代码         |       ❌        |        ✅         |
| 优先级 / 时间 / 教师过滤 |       ❌        |        ✅         |
| ⏰ 定时开抢              |       ❌        |        ✅         |
| 免 F12 / 免复制代码      |       ✅        |        ❌         |

---

## 注意事项与常见问题

1. **课程已经满员（余量为 0），可以抢吗？**
   - **可以，这就是本脚本的核心用途（捡漏）。** 即使名额为 0，点击该行右侧的【⚡ 抢这门】，脚本就会将该课程加入监控队列并在后台持续盯盘。一旦有其他同学退课释放出名额，脚本会在毫秒级自动提交抢课。
   - *提示：请务必在教务系统筛选条件中取消勾选“过滤已满课程”并点击“查询”，满员课程才会显示在表格中供你添加。*

2. **抢课运行期间有什么要求？**
   - **不要关闭选课页面标签页**，也不要退出登录。
   - 建议在电脑系统设置中将“睡眠”时间设为“从不”，防止电脑息屏休眠导致浏览器后台断网挂起。

3. **如何确认已经成功选上？**
   - 选上后，控制面板日志会输出醒目的绿色提示：`🎊 恭喜！成功选上课程:...`；同时该课程会自动从监控队列移出，教务系统表格对应行的状态也会更新为已选。

4. **误关了控制面板如何重新打开？**
   - 直接刷新选课页面，或者按 `F12` 打开浏览器控制台，在 Console 中输入 `hubuGrab.showUI()` 回车即可重新呼出面板。

5. **进入选课页面没有出现控制面板？**
   - 检查浏览器扩展设置中的“开发者模式”是否开启（Chrome / Edge 必开项）；
   - 检查油猴插件中的脚本是否处于开启状态；
   - 确认当前处于具体的选课列表页面（能看到课程表格的页面）。
